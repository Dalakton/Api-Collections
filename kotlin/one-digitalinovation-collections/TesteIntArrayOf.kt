package `one-digitalinovation-collections`

fun main() {

    val values = intArrayOf(2,3,4,5,6,10)

    values.forEach {
        println(it)
    }

    values.sort()
    values.forEach {
        println(it)
    }


}